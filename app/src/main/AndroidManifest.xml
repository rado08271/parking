<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="eu.parada.parking">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:theme="@style/AppTheme.BoughtParkingActivity"
            android:name=".view.BoughtParkingActivity"></activity>
        <activity
            android:theme="@style/AppTheme.BuyParkingActivity"
            android:name=".view.BuyParkingActivity" />
        <activity
            android:theme="@style/AppTheme.LicensePlateActivity"
            android:name=".view.LicensePlateActivity" />
        <activity
            android:noHistory="true"
            android:theme="@style/AppTheme.SplashActivity"
            android:name=".view.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>